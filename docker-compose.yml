services:
  db:
    image: mariadb:10.11
    container_name: peereval-db
    restart: always
    volumes:
      - ./mariadb_data:/var/lib/mysql
    environment:
      MARIADB_DATABASE: ${POSTGRES_DB}
      MARIADB_USER: ${POSTGRES_USER}
      MARIADB_PASSWORD: ${POSTGRES_PASSWORD}
      MARIADB_ROOT_PASSWORD: ${POSTGRES_PASSWORD}
    ports:
      - "3307:3306"
    healthcheck:
      test: ["CMD", "mariadb-admin", "ping", "-h", "localhost", "-u", "${POSTGRES_USER}", "-p${POSTGRES_PASSWORD}"]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 30s

  web:
    build: ./app
    container_name: peereval-web
    command: ["sh", "./entrypoint.sh"] 
    ports:
      - "80:5000"
    volumes:
      - ./app:/app
    environment:
      - DB_TYPE=mariadb
      - DB_NAME=${POSTGRES_DB}
      - DB_USER=${POSTGRES_USER}
      - DB_PASSWORD=${POSTGRES_PASSWORD}
      - DB_HOST=db
      - DB_PORT=3306
      - VM_IP=${VM_IP:-}
      - AUTO_UPDATE=${AUTO_UPDATE:-false}
    depends_on:
      db:
        condition: service_healthy

volumes:
  mariadb_data:
    driver: local